[build]
builder = "dockerfile"

[deploy]
startCommand = "alembic upgrade head && uvicorn leadify.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
